[build-system]
requires = ["setuptools>=68,<77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel-processor"
version = "0.2.0"
description = "Sentinel-2 L2A downloader with STAC, Fortran-backed validation, spectral indices, temporal statistics, band covariance, wavelet denoising, DL normalisation / tiling, and Plotly visualisation"
readme = "README.md"
license = {file = "LICENSE.md"}
requires-python = ">=3.10"

keywords = [
    "sentinel-2", "remote-sensing", "satellite", "stac",
    "rasterio", "plotly", "time-series", "phenology",
    "pca", "covariance", "wavelet", "denoising",
    "deep-learning", "normalisation", "tiling",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: GIS",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "pystac-client>=0.7",
    "rioxarray>=0.15",
    "xarray>=2023.1",
    "numpy>=1.24",
    "rasterio>=1.3",
    "plotly>=5.0",
]

[project.optional-dependencies]
netcdf  = ["netCDF4>=1.6"]
shapely = ["shapely>=2.0"]
postgis = ["geoalchemy2>=0.14", "shapely>=2.0"]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "netCDF4>=1.6",
]
all = ["netCDF4>=1.6", "shapely>=2.0", "geoalchemy2>=0.14"]

[project.scripts]
"sentinel-processor-compile" = "sentinel_processor._compile:main"

[project.urls]
Repository    = "https://github.com/niki8885/sentinel-processor"
Documentation = "https://github.com/niki8885/sentinel-processor/tree/master/docs"

[tool.setuptools.packages.find]
where   = ["."]
include = ["sentinel_processor*"]

[tool.setuptools.package-data]
sentinel_processor = [
    # Fortran sources (always shipped)
    "validation/fortran/*.f90",
    "indices/fortran/*.f90",
    "processing/fortran/*.f90",
    "filters/fortran/*.f90",
    "analysis/fortran/*.f90",
    "texture/fortran/*.f90",
    "wavelet/fortran/*.f90",
    "dl/fortran/*.f90",

    # Compiled libraries — included when present (built in setup.py)
    "validation/fortran/*.so",
    "validation/fortran/*.dll",
    "indices/fortran/*.so",
    "indices/fortran/*.dll",
    "processing/fortran/*.so",
    "processing/fortran/*.dll",
    "processing/fortran/libsentinel_timeseries*.so",
    "processing/fortran/libsentinel_timeseries*.dll",
    "filters/fortran/*.so",
    "filters/fortran/*.dll",
    "analysis/fortran/*.so",
    "analysis/fortran/*.dll",
    "texture/fortran/*.so",
    "texture/fortran/*.dll",
    "wavelet/fortran/*.so",
    "wavelet/fortran/*.dll",
    "dl/fortran/*.so",
    "dl/fortran/*.dll",

    # MSYS2 runtime DLLs bundled on Windows
    "validation/fortran/libgfortran*.dll",
    "validation/fortran/libgcc*.dll",
    "validation/fortran/libwinpthread*.dll",
    "indices/fortran/libgfortran*.dll",
    "indices/fortran/libgcc*.dll",
    "indices/fortran/libwinpthread*.dll",
    "processing/fortran/libgfortran*.dll",
    "processing/fortran/libgcc*.dll",
    "processing/fortran/libwinpthread*.dll",
    "filters/fortran/libgfortran*.dll",
    "filters/fortran/libgcc*.dll",
    "filters/fortran/libwinpthread*.dll",
    "analysis/fortran/libgfortran*.dll",
    "analysis/fortran/libgcc*.dll",
    "analysis/fortran/libwinpthread*.dll",
    "texture/fortran/libgfortran*.dll",
    "texture/fortran/libgcc*.dll",
    "texture/fortran/libwinpthread*.dll",
    "wavelet/fortran/libgfortran*.dll",
    "wavelet/fortran/libgcc*.dll",
    "wavelet/fortran/libwinpthread*.dll",
    "dl/fortran/libgfortran*.dll",
    "dl/fortran/libgcc*.dll",
    "dl/fortran/libwinpthread*.dll",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts   = "-v --tb=short"
markers   = [
    "slow:        mark test as slow (> 1 s)",
    "fortran:     requires compiled Fortran shared library",
    "integration: writes/reads real files via rasterio",
]

[tool.coverage.run]
source = ["sentinel_processor"]
omit   = ["*/fortran/*", "*/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "raise FileNotFoundError",
    "if __name__ == .__main__.:",
]